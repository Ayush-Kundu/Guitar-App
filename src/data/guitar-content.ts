import guitarContentJson from './guitar-content.json';

// Export the JSON content directly - this ensures the JSON file is the single source of truth
export const guitarContent = guitarContentJson;

export default guitarContent;
